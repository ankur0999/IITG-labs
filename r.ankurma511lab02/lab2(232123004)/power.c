#include<stdio.h>

int main(void){
    int a=1;
    printf("Power 1\tPower 2\tPower 3\tPower 4\tPower 5\n");
    printf("%d\t%d\t%d\t%d\t%d\n",a,a*a,a*a*a,a*a*a*a,a*a*a*a*a);
    a = ++a;
    printf("%d\t%d\t%d\t%d\t%d\n",a,a*a,a*a*a,a*a*a*a,a*a*a*a*a);
    a = ++a;
    printf("%d\t%d\t%d\t%d\t%d\n",a,a*a,a*a*a,a*a*a*a,a*a*a*a*a);
    a = ++a;
    printf("%d\t%d\t%d\t%d\t%d\n",a,a*a,a*a*a,a*a*a*a,a*a*a*a*a);
    a = ++a;
    printf("%d\t%d\t%d\t%d\t%d\n",a,a*a,a*a*a,a*a*a*a,a*a*a*a*a);
    a = ++a;
    printf("%d\t%d\t%d\t%d\t%d\n",a,a*a,a*a*a,a*a*a*a,a*a*a*a*a);
    a = ++a;
    printf("%d\t%d\t%d\t%d\t%d\n",a,a*a,a*a*a,a*a*a*a,a*a*a*a*a);
    a = ++a;
    printf("%d\t%d\t%d\t%d\t%d\n",a,a*a,a*a*a,a*a*a*a,a*a*a*a*a);
    a = ++a;
    printf("%d\t%d\t%d\t%d\t%d\n",a,a*a,a*a*a,a*a*a*a,a*a*a*a*a);
    a = ++a;
    printf("%d\t%d\t%d\t%d\t%d\n",a,a*a,a*a*a,a*a*a*a,a*a*a*a*a);
    return 0;
    }
